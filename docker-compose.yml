version: "3.8"

services:
  api:
    build: .
    container_name: lerka-chat-backend
    ports:
      - "3016:8000"
    env_file:
      - .env
    volumes:
      - ./app:/app/app
      - ./main.py:/app/main.py
    restart: unless-stopped
